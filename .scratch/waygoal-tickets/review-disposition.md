# 对抗性审查后的修订

用户已确认保留 10 张实现票及原阻塞边。父 Spec #1 不修改。

- T1 复用现有宿主与验证骨架，补可控模型验证；持久化从一开始按工作区隔离。
- T2 复用只读历史 API，修正界面查看与继续的混用；不接受把内部 tree 的可见性强制限制在聊天面板。
- T4 复用命名和搜索；tips 移到 T7。
- T6 先实现明确支持格式的本地扫描读取，不假设任意 tracker 格式通用，也不要求 Agent 再通知／抄写。
- T7 复用无提示新建与草稿，替换旧的一票单绑定、长提示和阻塞票禁聊行为；添加实际体验走查。
- T8 扩展已有阻塞判断，不重建图执行器。
- T9 先在真实 skill 路径验证远程接入触发，再交付来源引用读取；自定义 tracker 仅离线样本。

推荐执行顺序：T1 → T2 → T6 → T7 → T8 → T4 → T3 → T5 → T10 → T9。这是优先级建议，不额外增加依赖。

已检查 50 条用户故事仍全部覆盖，所有依赖均指向前序票。
