# Local dependencies and generated output
node_modules/
.next/
out/
build/
coverage/
test-results/
*.tsbuildinfo
.DS_Store
*.log

# Credentials and machine-specific session bindings
.env*
*.pem
.beacon-prototype/

# Keep the prototype's existing upstream checkout local.
prototypes/pi-web/.git

# Local request payload and pre-publication verification backup
.scratch/waygoal-tickets/request.json
.scratch/waygoal-tickets/parent-before.json
