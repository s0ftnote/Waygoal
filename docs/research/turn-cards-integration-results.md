# 连续聊天与真实 Pi 轮次接入

2026-09-15。接入依据是 [ADR 0006](../adr/0006-continuous-chat-and-turn-cards.md) 和[交互基线](../design/canvas-navigation-and-states.md)，原型分支没有整体合并。

## 改变的体验

服务于“保持方向”：右侧沿用 ChatWindow、ChatInput、快捷节点导航和 Pi 运行能力；左侧展示会话的轮次树。点击当前路径卡片定位原文，其他路径在画布内预览。切回总画布可以继续整理会话、分组与票据，当前聊天仍保留。

Tree 和消息编辑共用 Waygoal 的明确路径切换入口。切换成功后恢复该路径的文字、图片与待发送引用；失败或取消不替换当前聊天。助手消息的分叉包含该消息，用户消息的分叉从其 parent 开始，并将原文交回输入框。会话间继续也由用户明确选择。

## 真实来源与展示

- `src/features/sessions/turns.ts` 投影未压缩的 Pi entries，身份使用 session ID 与原始 entry ID。编号只用于展示，不承担身份。用户消息开始轮次，工具过程归入同轮；压缩和分支摘要分别显示。回答中再次分叉时，新路径显示延续卡片，不拼接兄弟路径的回答。
- 历史读取仍走宿主 context 接口，支持多页定位及压缩前原文。模型有效上下文另由 Pi 的 `buildContextEntries` 决定，两者不混用。
- Pi `0.85.1` 在 `message_end` 通知之后同步追加记录。宿主保留原事件顺序，随后补发该消息对应的 entry ID，使运行中已经保存的用户消息也能定位。
- 轮次位置、手动关联和独立预览位置保存在原有画布记录中。聊天仍使用 Pi 会话文件，不为卡片创建会话或票据。

## 引用范围与发送

本版明确提供整轮文字、最终回答、用户消息、完整路径文字及回答原文摘录。文字范围包含可见的工具结果文字，但不包含图片、思考内容和工具调用参数；界面在选择时说明范围，不生成这些内容的替代描述。需要图片时继续使用原有聊天附件能力。

材料在用户选取时读取并冻结。审阅区显示实际发送的原文；发送经过原有 prompt 通道，用户消息同时保留材料文字及 session、entry、范围和取得时间。后续改选不会修改旧消息。相同会话有效上下文已有的条目不重复加入；被压缩掉的原文可重新引用。跨会话复制的 entry ID 不猜测为同一来源。重叠材料在审阅区去重，预览和实际发送使用同一份结果。

读取材料期间开始发送、切换路径或离开当前聊天，会取消尚未完成的读取，过期响应不进入其他路径草稿。材料准备后若实际继续位置改变，宿主拒绝使用过期位置发送，保留输入，提示重新选择。普通聊天不要求选择材料；“仅作关联”不进入 prompt。

## 验证入口

- `npm run check`：静态检查、类型及全部单元测试。
- `npm run test:waygoal-branches`：连续聊天、双向定位、分支预览、Tree 草稿恢复、材料实际发送、真实分叉及窄屏检查。
- `npm run test:e2e`：全部 Waygoal 浏览器回归，包括原有票据、目录、多画布、分组与来源能力。
- 本轮截图与检查结果位于 `apps/web/test-results/waygoal/turns/`；它们是隔离宿主与可控假模型的结果，不覆盖旧研究图片。

## 连续聊天首轮验证结果

在 Node.js 24.21.0、Pi SDK 0.85.1 下：

- 静态检查及类型检查通过；单元测试 1,101 项通过。lint 仅保留未改动的旧实验文件中的 unused variable 警告。
- `npm run build` 生产构建通过；未改动的会话导出路由保留动态依赖编译警告。
- Waygoal 全部 10 组浏览器检查累计 248 项通过，其中连续聊天接入 26 项。前 8 组由整套命令完成；地图组修正实际导航入口后，与远程组分别复跑通过。
- 定向回归覆盖 300 轮历史跨多页定位、发送后保留已加载 DOM、慢回复期间定位、空会话分叉、两条路径的文字与材料草稿往返，以及延迟材料响应不串入新路径。
- Standards 与 Spec 两轴审查提出的问题均已修复并复核；桌面与窄屏截图已查看。材料只使用可控假模型验证实际请求，不据此声称真实供应商体验已验收。

## 使用范围

沿用进程内 Pi 宿主及原有会话写入限制。本轮模型调用使用隔离数据和可控假模型，没有使用个人现有会话或付费模型。输入草稿沿用宿主浏览器内存的保存方式；页面刷新后的草稿恢复不在本轮承诺中。发送后的引用来源及布局可重新读取。超大树的虚拟化、图片材料引用和视觉参数仍需真实日常使用验证。总画布沿用的多地图提示可能遮住另一张票据及其附属按钮；键盘聚焦后按 Enter 及已打开票据的面板入口可用，本轮未调整该提示布局。地图内容回归因此使用真实键盘入口，不代表重叠已修复。


## 跨会话共同画布接入

2026-09-15 基线的后续实现，依据用户“落代码”授权。服务于“保持方向”和“理清想法”：同一画布中的会话轮次同时呈现，选择查看对象不再过滤掉其他讨论；右侧继续使用原有连续聊天。

- `turn-board.ts` 负责只读投影。会话及 entry 组成卡片身份；只有明确的分叉祖先、完整可见条目 SHA-256 摘要和父卡片关系均一致时，才共享前缀展示。工具消息的完整内容参与校验。共享卡片保留每个会话的原始身份，不改变 Pi 历史或有效上下文；只有来源会话、没有具体 entry 的旧记录不会猜测合并。
- 连续历史、分叉、已发送引用及手动关联分别绘制，支持鼠标和键盘查看关系。待发送引用指向当前对话下一轮，发送后指向实际使用轮次；从右侧原消息的“引用来源”也能打开原文。
- 引用元数据只存正文范围，不额外复制一遍正文。旧消息没有范围时，明确展示那一轮全部引用原文。来源暂时不可读时仍能查看已发送快照，读取失败会明确提示；不会用重新读取的内容替换旧材料。
- 共同布局和跨会话关联按工作区、画布保存；读取兼容旧的单会话布局。新增卡片避开其他会话的已保存位置。每批最多并发读取四段会话，后台页面暂停轮询。

### 本轮验证

- 静态检查、类型检查及全部 1,108 项单元测试通过；排序稳定性调整后，投影、SDK 分叉摘要及材料处理的 13 项定向测试复跑通过。
- 真实 Pi SDK 加可控假模型的连续聊天套件 41 项通过：包含多会话同时显示、共享前缀、跨会话引用真实发送、关系查看不动草稿、布局拖动刷新恢复、来源读取失败、窄屏和 300 轮历史。
- 票据讨论套件 26 项通过，覆盖多讨论分叉、只读回看、收起及宿主重启。
- 生产构建通过；会话导出路由原有的动态依赖编译警告仍保留。
- 桌面及窄屏截图已查看。结果保存在 `apps/web/test-results/waygoal/turns/`，其中 `05-shared-board.png` 展示共同画布。

这些检查不代表真实模型供应商或长期大规模工作空间已完成体验验收；大量会话的增量读取、图虚拟化和展开收起仍需后续打磨。输入草稿继续沿用现有会话内存机制，不新增刷新恢复承诺。

## B 布局、空画布入口与会话归类

2026-09-16，按用户选定的 B 原型落到真实 Pi 宿主，服务于“理清想法”和“保持方向”。

- 工作目录与画布管理收进左上菜单，右侧聊天保持浮窗。轮次导航放在左下，分叉与引用操作跟随选中的卡片，Escape 和点击空白会收起。
- 尚无会话的画布只显示居中输入框，即使目录里有票据文件也如此。需要票据时从工作区菜单选择“会话与票据”。首次发送落下真实轮次后，同一聊天组件移到右侧，画布与第一个节点显现；支持系统减少动态效果设置。
- 继续复用工作区记录中的 `sessionCanvas`。未归类历史会话进入默认主画布；新画布保持为空。菜单中的“导入已有会话”可搜索同一工作目录的其他会话并移入，显示来源画布；修改归属不复制或改写 Pi 文件、不改变继续路径、不调用模型。可通过同一入口移回。服务端拒绝跨工作目录的会话归类。
- Pi Web 自身的新会话欢迎信息默认保留；Waygoal 嵌入时隐藏欢迎标题与版本号，只呈现输入区。

交互确认与实现记录分开保留：对比原型分支 `codex/prototype-ux-clarity-20260916` 的 `f59018e` 记录选择 B；真实浏览器检查使用隔离 Pi 目录与可控假模型。

### 此次验证

- lint 和类型检查通过，全部 1,108 项单元测试通过；lint 仅保留旧实验中的 `_entries` 警告。
- 10 组浏览器回归共 273 项通过。基础组先完成；其余套件按更新后的菜单路径分别复跑，最终逐组检查 `checks.json`，没有保留失败项。
- 连续聊天组 46 项包含：打开空画布不创建 Pi 会话、首次发送失败保留入口与草稿、首次真实轮次展现时不替换输入框、卡片操作跟随卡片与 Escape 收起，以及原有引用、分叉、窄屏和 300 轮历史检查。
- 工作区组 27 项包含：有票据文件的新画布仍先呈现输入框、归属独立、导入与刷新、移回、Pi 文件逐字节不变、无模型调用，以及跨目录导入拒绝。
- 桌面空入口、首轮展开和窄屏截图已查看。证据位于 `apps/web/test-results/waygoal/`，未覆盖历史研究图片。
- 生产构建通过；原有会话导出路由的动态依赖编译警告仍保留。

### 2026-09-16 · 入口、卡片与面板尺寸

- 真实页面加入桌面聊天面板的宽、高和左上角拖拽调节；浏览器本地记忆尺寸，支持双击 / Home 复位与方向键微调。窄屏仍使用自适应布局。
- 实页手动验证：面板由 512×622 调到 622×622，再调为 552×492；调整期间未发送草稿保留，刷新后尺寸仍为 552×492，Home 恢复默认。仅验证拖拽时草稿连续，不将刷新后的草稿恢复算作本次能力。
- 入口取消“主画布”显示和新建前的命名表单，历史默认归属显示为“未归类会话”。目录、加入会话分别在子页操作。轮次卡片收整排版、单层选中边界和紧凑操作条，下一轮提示取消重复长标题。
- 验证：类型检查通过；lint 无错误（历史实验脚本有 1 个 unused warning）；工作区浏览器套件 27/27 通过，覆盖新建不发送、不创建 Pi 会话，切换与重启恢复，导入归属持久化、可逆归类和跨目录拒绝。日志 `/tmp/waygoal-entry-check.log`，证据 `apps/web/test-results/waygoal/workspaces/`。
- 真实 Pi 页面已检查菜单和选中卡片呈现；此记录不代表所有 UI 的设计验收已完成。预览服务恢复在 30142。
