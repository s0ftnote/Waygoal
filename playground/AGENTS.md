# Beacon integration playground

This is a fictional small screening event used to try the Beacon interface. It is not a real event or an accepted user plan. Speak Chinese.

Use the local Markdown tracker at `.scratch/screening/map.md` and `.scratch/screening/issues/NN-*.md`. Read `/Users/neuron/.agents/skills/setup-matt-pocock-skills/issue-tracker-local.md` for the conventions. Keep `Type:`, `Status:`, `Blocked by:` lines and `## Question` / `## Answer` headings.

This working directory does not require Git. Research findings can be saved to `research/*.md` without branches. Research subagents must use `openai-codex/gpt-5.6-luna`. Keep investigations small and use primary sources. Do not run other models.

Grilling and prototype tickets require the human's actual reply. Never simulate the human or resolve such a ticket by yourself. Stop after the current ticket; new clear questions become new tickets, not automatic extra conversations. Do not contact people or publish anything.
