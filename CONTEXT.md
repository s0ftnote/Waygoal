# Waygoal

Waygoal 帮助用户理清想法、保持方向，并依据已形成的判断推进与验证。这里定义讨论产品和工作流时使用的共同语言。

## Language

**工作区（Workspace）**：
以用户选定的工作目录为基础组织工作的范围，可包含多张画布。
_Avoid_: 将工作区等同于 Git 仓库或一张画布

**目的地（Destination）**：
一张地图希望达到的明确结果，也是判断问题是否属于这次探索的范围依据。
_Avoid_: 用当前会话结束代替地图完成

**总画布（Canvas）**：
工作区内呈现会话、真实分叉及可选工作记录之间关系的导航空间，可以包含多个互不相连的起点，不以使用某个 skill 或存在票据为前提。一个工作区可以有多张，各有稳定身份，名字相同也是不同的两张；一段会话属于其中一张。
_Avoid_: 将总画布等同于 Wayfinder 地图；把同一段会话同时摆在多张画布上；用名字当作画布的身份

**Wayfinder 地图（Wayfinder map）**：
围绕一个目的地组织探索的共享索引，包含已有决定、尚未明确的方向和范围边界，并关联相应票据。
_Avoid_: 聊天记录合集、预先固定的全部实施步骤

**讨论问题（Discussion question）**：
当前讨论中需要澄清的一项疑问或取舍，承接相关前提、选项、回答与依据；它可以在一次连续讨论中解决，也可以成为独立票据所处理的问题。
_Avoid_: 将每次追问或每个选项等同于一张票据

**决策树（Design tree）**：
Grilling 对讨论问题及其前提关系的组织，随着用户回答和新事实改变；它表达 Agent 当前对问题结构的理解，不保证已经识别全部依赖。
_Avoid_: 与 Wayfinder 票据地图等同、预先固定的问卷

**决策票（Decision ticket）**：
Wayfinder 中需要通过讨论、研究、试做或准备工作来解决的独立问题；它的完成为后续判断提供依据。
_Avoid_: 将决策票当作实现任务

**执行票（Implementation ticket）**：
依据已明确方案开展的一项工作，包含足以推进和验证完成的要求及依赖。
_Avoid_: 将关闭决策票等同于完成执行票

**票据（Ticket）**：
Tracker 中一件待解决问题或待完成工作的正式记录，具有来源身份、内容、状态及相关关系；决策票和执行票是不同用途的票据。
_Avoid_: 将票据等同于一段聊天

**Tracker**：
用户选定的正式工作记录来源，承载地图、票据及其有效结论和依赖。
_Avoid_: 将 Waygoal 的展示副本当成另一套正式 tracker

**节点（Node）**：
画布上可定位和进入的单元，可以承载会话或真实分叉，也可以关联实际票据、相关讨论和结果；节点不必有票据。
_Avoid_: 用聊天次数定义节点数量

**会话（Session）**：
用户与 Pi Agent 讨论并推进工作的上下文和历史；可以独立存在，也可以作为票据下的讨论。票据是关联讨论的上层工作单元，其持续存在不依赖某个会话一直存活。

**宿主（Host）**：
为用户呈现总画布并运行、展示会话的本地程序。
_Avoid_: 把宿主重启等同于会话丢失

**接入扩展（Waygoal extension）**：
装进 Pi 里的那部分 Waygoal，把画布和会话、工作记录连接起来，不另加工作流规则。
_Avoid_: 把接入扩展当作画布界面本身、把它当作另一套工作流规则

**票据关联（Ticket association）**：
表明一段会话围绕哪张票据开展讨论的关系；一张票据可以尚无会话，也可以关联多个会话及其讨论分支。
_Avoid_: 票据阻塞依赖、会话分叉来源、聊天上下文合并

**真实分叉（Conversation branch）**：
从已有讨论中的某条消息分出、可以和原讨论同时各聊各的另一段会话，保留与来源的关系。要两个方向互不打扰地并行推进，用的就是它。
_Avoid_: 每轮追问、票据依赖、仅为排版拆出的卡片

**路径（Path）**：
一个分叉点下一段可以直接接着聊的对话历史；同一段会话可以同时留着多条，点开哪条就在哪条里说话。多条路径共用同一段会话，同一时刻只有一条在被继续；要两条同时各聊各的，那是真实分叉。
_Avoid_: 位置或指针、把「点开」当成一次切换操作、另一段会话、界面上的排版分栏

**会话标题（Session title）**：
用于识别、查找和整理一段会话的可变名称，可由用户命名或请求生成；名称变化不改变会话身份。
_Avoid_: 票据标题、由标题推断的关系、另存一份画布专用会话名称

**画布分组（Canvas group）**：
用户在画布中将若干节点归在一起并命名的整理范围，表达这些内容在自己的工作中属于一组。概览时看得见组名，展开进去还是原来的节点；一个节点同时只属于一个分组，分组只在它所在的那张画布上。
_Avoid_: 新会话、Wayfinder 地图、共同上下文、按标题或内容自动归类

**手动关联（Manual association）**：
用户为方便整理和回看而建立的节点联系，可以附一句关联说明。它的来源就是用户这么说了，不由模型推断，也不声称方向；建立或删除都不改 Pi 历史与票据正文。
_Avoid_: 真实分叉来源、票据阻塞关系、票据关联、上下文合并、由模型推断的关系

**依赖关系（Blocking）**：
一个问题或工作在满足另一项前提后才能继续完成的关系。
_Avoid_: 把所有连线都解释为父子会话、把尚不能完成等同于不能讨论

**解锁（Unblocked）**：
一项工作原有的阻塞前提全部得到满足，从等待前提变为可以继续推进的状态变化。
_Avoid_: 下游自动完成、自动开始下一个会话

**分支来源（Branch origin）**：
一段独立探索从哪段讨论、因为什么问题分出的关系；需要返回时，它也说明原讨论尚待继续的内容。
_Avoid_: 将分支来源等同于阻塞关系

**原文快照（Source snapshot）**：
最近一次成功取得的票据原文及相关来源记录的本地副本，保留来源与取得时间；它反映已知版本，不代表来源此刻必然未变。
_Avoid_: AI 改写稿、永久实时的原票据

**预览（Preview）**：
对地图或票据现有信息的简要展示，用于识别当前问题和进展；其内容能够回到对应原文。
_Avoid_: 第二份独立维护的票据正文

**产物（Artifact）**：
探索过程中真实产出并存放在工作目录里的文件，如方案草稿或记录；来源明确指向它时可以打开查看，Waygoal 不改写也不另存一份。
_Avoid_: 由聊天内容重写的摘要、Waygoal 自己维护的第二份产物

**来源票据（Remote ticket）**：
不是本工作目录自己的文件、而是从外部来源读回来的票据；它在画布上和本地票据一样是一张卡片，只读，正文是来源自己的原文。
_Avoid_: 由 Agent 复述或改写的正文、Web 侧另存的第二份正式正文、另建的平台会话模型

**未同步（Not synced）**：
来源那边已经读到了，画布上却还没有这张票原文时的状态；它只说这一件事，不显示任何冒充来源的内容，可以重新取得。
_Avoid_: 把未同步当作来源那边失败、用上一次或猜测的内容顶替

**完整视图（Full view）**：
展示已取得的完整票据原文、相关结论记录和来源信息的视图，关联的会话与产物可进一步打开；未取得的内容不算已完整读取。

**画布记录（Canvas record）**：
Waygoal 为恢复用户工作现场保留的节点身份、会话关联、布局和原文快照等记录。
_Avoid_: 将画布布局当作 tracker 中的工作事实

**辅助提示（Guidance）**：
解释技能用法或界面概念的可选短说明，以整理好的方法建议为内容，依据明确入口或用户操作呈现。
_Avoid_: 聊天内容分析、用户已确认的结论、票据原文

**检查提示（Check prompt）**：
一张地图完整的票据集合已取得、非空且全部关闭时出现的可关闭短提示，提醒用户核对目的地与尚未明确的方向；它标记的是检查时机，可关闭也可重新打开。
_Avoid_: 地图已完成的结论、按叶子或当前视野统计、把取消与移出范围当作成功结论
