# Design System: Waygoal

## 1. Visual Theme & Atmosphere

以 [一口新饭](https://www.onenewbite.com/) 及用户提供的两张截图为配色参考：纸白、浅青、深海军蓝与少量朱红。浅色与深蓝提供安定的阅读底子，朱红形成温暖、活泼的行动焦点。Waygoal 的视觉重心是非线性聊天画布，避免把营销站的大色块、超大标题和插画密度直接搬进工作界面。

本轮规范浅色主题；四种基础色取自参考网站，功能派生色与比例是 Waygoal 首版设计选择，后续通过原型验证。字体、尺寸与动画参数是起始建议，不代表已经定稿或接入产品。交互需求仍以[画布导航与状态](docs/design/canvas-navigation-and-states.md)为准。

### 参考观察：颜色与形态一起看

以下根据用户截图及 2026-09-09 网站实际样式核对，不是新增的 Waygoal 交互要求：

- 同样是“加入我们”，顶部导航使用深蓝，小幅邀请区使用朱红；红色随场景强调邀请，不固定对应所有主要按钮。当前预览将“从这里继续”保留为深蓝，把朱红用于独立的试用邀请区；这是按场景试配，不是固定功能到颜色的规则。
- “新饭故事／活动”是深蓝胶囊、浅青文字，右侧另放浅青圆形箭头；按钮内部的颜色呼应浅青背景，不只是统一的白字实心按钮。
- 故事区外容器的圆角约 41px，红色按钮约 20px；截图中的卡片也有宽圆角。亲切感来自较柔和的形状与留白，不依赖大阴影或玻璃效果。
- `JOIN / READ / EXCHANGE` 和 `UPCOMING EVENTS` 的字体声明为 Black Han Sans，分别约 28px 与 40px；正文声明为 Inter 加回退字体。厚实展示字、普通正文与细线插画形成粗细对照；不能把 skill 的字体禁令误写成参考网站的事实。
- 邀请区的白色波浪线、手绘短线和深蓝人物线稿提供轻松感；纸白卡片嵌入浅青区域，浅青区域又落在纸白页面上，通过底色交替形成层次。动态资源本次未完整核验，不据静态截图推断动画行为。

对 Waygoal 的启发是保留冷暖对照、柔和形状和清楚的轻重关系；具体哪些按钮用红、是否使用手绘装饰，要在工作界面中验证。仅换色不能宣称已经还原这份参考的气质。

## 2. Color Palette & Roles

2026-09-09 核对网页样式与截图主像素，二者均包含 `#F6FAF6`、`#A9DBDC`、`#1D365A`。参考站的“加入我们”使用朱红 `#E53A46`，与浅青大底形成冷暖对照；深蓝胶囊按钮承载其他入口。红色在参考中是主动邀请，不是异常提示。Waygoal 保留这种角色分工。

| 名称 / token | 色值 | 用途 |
| --- | --- | --- |
| 纸白 / `--wg-canvas` | `#F6FAF6` | 无限画布、页面底色，占据主要视野 |
| 浮层白 / `--wg-surface` | `#FFFFFF` | 聊天面板、菜单、普通节点，与画布形成轻微层次 |
| 浅青 / `--wg-accent` | `#A9DBDC` | 空间与选择强调；选中节点局部、选项底色、轻量说明 |
| 浅青薄雾 / `--wg-accent-soft` | `#E8F4F2` | 悬停底色、关联路径所在区域，派生色 |
| 深海军蓝 / `--wg-ink` | `#1D365A` | 正文、标题、图标、主要操作、焦点轮廓 |
| 朱红 / `--wg-action` | `#E53A46` | 少量重要行动入口，形成冷暖对照；不用于节点完成或阻塞状态 |
| 次级蓝灰 / `--wg-muted` | `#586A76` | 时间、元信息、次级说明，派生色 |
| 淡边线 / `--wg-border` | `#D7E5E1` | 分隔与装饰边界；不单独承担交互识别 |
| 路径蓝灰 / `--wg-path` | `#597B82` | 实际关系线、必要的控件边界，派生色 |
| 异常暗红 / `--wg-danger` | `#AA3540` | 删除、操作失败等语义；不是品牌装饰色，配文字或图标 |

配色重量：纸白与白色占据主要视野，浅青承托局部空间，深蓝负责文字与结构，朱红只点在少量值得关注的行动上。同一视野优先突出一个朱红操作，不让它与当前节点争夺注意。比例通过使用场景调整，不机械指定百分比。对话密集时进一步减少大面积青色，保留阅读安静区。

**阅读与状态：** 深蓝在纸白上对比度约 11.53:1，在浅青上约 8.02:1；次级蓝灰在纸白上约 5.33:1。浅青上的白字仅约 1.52:1，不用于正文或按钮文字。浅青底使用深蓝字；深蓝按钮使用纸白字。路径蓝灰在纸白上约 4.35:1，适合关系线，正文仍用上述文字色。朱红配白字约 4.18:1，适用于足够大的粗体按钮文字；本轮预览采用 19px / 700。小字号按钮应另选经过对比度核验的深红派生色，不能直接照搬。数值由 sRGB 相对亮度计算。

**状态映射：** 当前节点用深蓝焦点轮廓和浅青局部填充；阻塞保留可读文字与轮廓；解锁一次缓慢点亮；已有结论用稳定标记和来源状态文案。取消与解决明确区分。功能状态不再扩出一套五颜六色的票种标签。提示与错误依靠文字、图标共同表达。

规范的 CSS token 位于 [waygoal-theme.css](docs/design/waygoal-theme.css)，[主题预览](docs/design/theme-preview.html)演示配色在节点、聊天和按钮中的应用；其中节点与对话是虚构样例，未连接 Pi 或 tracker。

## 3. Typography Rules

首版排版建议：中文使用 `PingFang SC / Noto Sans SC`，西文优先 `Geist`，后接系统无衬线字体；代码使用 `SFMono-Regular / Consolas` 等宽字体。预览使用本地回退字体，不外发字体请求。

正文 15–16px，行高 1.65；元信息 13–14px；节点标题 16px / 600；界面标题 24–32px / 600。中文正文每行约 28–40 字，聊天长文本控制在 65ch 左右。层级主要靠字重、位置和留白；不复制参考站厚重的大写展示字体到聊天正文。

## 4. Component Stylings

- **节点：** 纸白或白底、约 20–24px 圆角；通过深蓝轮廓标示选中，克制使用阴影。预览用浅青外容器包住纸白画布与聊天面板，形成底色交替；产品是否使用同样面积的浅青仍需实际画布验证。
- **按钮：** 少量优先行动采用朱红底配白字；常规操作沿用深蓝底配纸白字，浅青底配深蓝字为局部选择；普通操作使用白底或透明底。深蓝胶囊可配浅青文字及圆形箭头，呼应周围底色。按下位移 1px，焦点轮廓 2px；触控目标至少 44px。
- **输入与聊天：** 内容区域以白与纸白为主，浅青只作轻微分组。输入控件具有可辨认边界，标签与报错不靠颜色独立传达。
- **提示：** 浅青薄雾底、深蓝字、可关闭，视觉低于当前讨论；不改变消息与焦点。

## 5. Layout Principles

无限画布保留空间关系，节点之间留出连线和移动余量；工具栏、面板与正文保持清楚层级。画布允许平移缩放，不能套用营销页面“所有内容必须单列”的规则。

窄屏下控件和色板单列，聊天使用可返回画布的独立面板。界面外壳无意外横向溢出；画布的有意平移不等于页面溢出。具体面板位置、节点展开方式通过交互原型选择，本文件不重新定义工作流程。

## 6. Motion & Interaction

动效用于保留方位：分叉连接、节点展开、返回来源、已核实的状态变化。轻交互约 120–180ms；视野与面板转场约 240–360ms；一次性点亮可试 700–1000ms。采用自然减速，弹簧可从 stiffness 100 / damping 20 试起。

只动画 transform / opacity；光效用静态柔光层改变透明度，避免持续重绘大范围阴影。保留“缓慢发光”的方向，但在浅色主题中使用纸白与浅青的低强度光晕，不新增霓虹色。只有真实状态变化才播放，不让所有节点循环呼吸。用户可继续操作；`prefers-reduced-motion` 下直接呈现最终位置与状态。

## 7. Anti-Patterns & Scope

- 不将浅青用作浅底上的小字，不使用白字浅青按钮。
- 不引入紫蓝霓虹、纯黑正文、彩虹票种或大面积竞争注意的红色。
- 不自动给所有节点播放浮动、闪烁或持续呼吸，不用动画制造虚假的执行进度。
- 不照搬营销站的大幅色块、超大标题和强制卡片网格到聊天画布。

本文件依照用户指定的 `stitch-design-taste` 组织。用户参考与既有产品决定优先于该 skill 的通用预设：保留浅青与朱红的角色分工，不套用“最多一个强调色”；保留带绿色的纸白而非替换成 Zinc；保留低强度状态点亮而非霓虹外发光；不采用所有活跃组件无限动画、营销 Hero 或画布单列化。此次未调用 Google Stitch，也未修改 Matt skills、Pi 运行逻辑或当前产品主题。
